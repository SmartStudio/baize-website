Use `ContactForm` at the bottom of service pages and the contact page.

```jsx
<ContactForm title="让我们帮你的团队建立第一套 AI 工作流" />
```

Keep the form short. Ask for the current problem rather than forcing users through a long qualification flow.
